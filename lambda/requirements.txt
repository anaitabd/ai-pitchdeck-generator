# AWS Lambda function dependencies for AI Pitch Deck Generator
# Python 3.12 compatible

# AWS Lambda Powertools (logging, tracing, validation)
aws-lambda-powertools==2.31.0

# Data validation
pydantic==2.5.3

# AWS SDK
boto3==1.34.19
botocore==1.34.19

# HTTP client for callbacks (modern, async-capable)
httpx==0.26.0

# HTTP client for presigned URL downloads
requests==2.31.0

# Anthropic SDK for Claude Sonnet 4.5
anthropic==0.8.1

# ============================================================================
# Optional: Document Parsing Libraries (uncomment if needed)
# ============================================================================

# For PDF text extraction (recommended: use pdfplumber or pypdf)
# Note: pdfplumber has better text extraction but larger package size
# pdfplumber==0.10.4

# Alternative PDF library (smaller, faster, but less accurate)
# pypdf==3.17.4

# For Word document extraction (.docx files)
# python-docx==1.1.0

# For character encoding detection (optional, useful for mixed encodings)
# chardet==5.2.0

# ============================================================================
# Production PDF/DOCX Support:
# To enable PDF/DOCX support, uncomment the library above and update
# the extract_text_from_document() function in handler.py with:
#
# For PDF (using pdfplumber):
#   import pdfplumber
#   from io import BytesIO
#   with pdfplumber.open(BytesIO(content)) as pdf:
#       text = "\n".join(page.extract_text() or "" for page in pdf.pages)
#
# For DOCX (using python-docx):
#   from docx import Document
#   from io import BytesIO
#   doc = Document(BytesIO(content))
#   text = "\n".join(paragraph.text for paragraph in doc.paragraphs)
# ============================================================================

